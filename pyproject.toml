[project]
name = "pid-chase"
version = "0.1.0"
description = "A hands-on educational game for learning PID control"
requires-python = ">=3.11"
dependencies = [
    "pygame>=2.5.0",
]

# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"
